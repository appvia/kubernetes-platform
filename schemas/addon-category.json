{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Addon category",
  "description": "Shared taxonomy for platform Helm and Kustomize addons (docs + validation)",
  "type": "string",
  "enum": [
    "gitops",
    "security",
    "networking",
    "observability",
    "cost",
    "storage",
    "compute",
    "workloads",
    "aws",
    "platform"
  ]
}
